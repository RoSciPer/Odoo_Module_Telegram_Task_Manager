<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Telegram User Form -->
    <record id="view_telegram_user_form" model="ir.ui.view">
        <field name="name">telegram.user.form</field>
        <field name="model">telegram.user</field>
        <field name="arch" type="xml">
            <form string="Telegram User">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="telegram_id"/>
                        <field name="username"/>
                        <field name="is_admin"/>
                        <field name="active"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Telegram User List -->
    <record id="view_telegram_user_list" model="ir.ui.view">
        <field name="name">telegram.user.list</field>
        <field name="model">telegram.user</field>
        <field name="arch" type="xml">
            <list string="Telegram Users">
                <field name="name"/>
                <field name="telegram_id"/>
                <field name="username"/>
                <field name="is_admin"/>
                <field name="active"/>
            </list>
        </field>
    </record>

    <!-- Action -->
    <record id="action_telegram_user" model="ir.actions.act_window">
        <field name="name">Telegram Users</field>
        <field name="res_model">telegram.user</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
