<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Task Manager Form -->
    <record id="view_task_manager_form" model="ir.ui.view">
        <field name="name">task.manager.form</field>
        <field name="model">task.manager</field>
        <field name="arch" type="xml">
            <form string="Task">
                <header>
                    <button name="action_start" type="object" string="Start" class="oe_highlight" 
                            invisible="state != 'draft'"/>
                    <button name="action_complete" type="object" string="Complete" class="oe_highlight" 
                            invisible="state not in ['draft', 'in_progress']"/>
                    <button name="action_cancel" type="object" string="Cancel" class="btn-danger" 
                            invisible="state == 'completed'"/>
                    <button name="action_reset_to_draft" type="object" string="Reset to Draft" 
                            invisible="state == 'draft'"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,in_progress,completed"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="title" placeholder="Task Title..."/>
                        </h1>
                    </div>
                    
                    <group>
                        <group>
                            <field name="assigned_user_id"/>
                            <field name="telegram_user_id"/>
                            <field name="vehicle_id"/>
                            <field name="category_id"/>
                        </group>
                        <group>
                            <field name="priority"/>
                            <field name="date_deadline"/>
                            <field name="date_start" readonly="state == 'draft'"/>
                            <field name="date_completed" readonly="1"/>
                            <field name="progress"/>
                        </group>
                    </group>
                    
                    <notebook>
                        <page string="Description">
                            <field name="description" placeholder="Task description..."/>
                        </page>
                        <page string="Time Tracking">
                            <group>
                                <field name="estimated_hours"/>
                                <field name="actual_hours"/>
                                <field name="is_overdue"/>
                                <field name="days_to_deadline"/>
                            </group>
                        </page>
                        <page string="Tags &amp; Notes">
                            <group>
                                <field name="tag_ids" widget="many2many_tags"/>
                                <field name="internal_notes"/>
                            </group>
                        </page>
                        <page string="Telegram">
                            <group>
                                <field name="telegram_message_sent" readonly="1"/>
                                <field name="telegram_chat_id" readonly="1"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <!-- Task Manager List -->
    <record id="view_task_manager_list" model="ir.ui.view">
        <field name="name">task.manager.list</field>
        <field name="model">task.manager</field>
        <field name="arch" type="xml">
            <list string="Tasks" default_order="priority desc, create_date desc">
                <field name="title"/>
                <field name="assigned_user_id"/>
                <field name="telegram_user_id"/>
                <field name="vehicle_id"/>
                <field name="priority"/>
                <field name="state" decoration-success="state == 'completed'" 
                       decoration-warning="state == 'in_progress'" decoration-danger="is_overdue"/>
                <field name="date_deadline"/>
                <field name="progress"/>
                <field name="is_overdue" invisible="1"/>
            </list>
        </field>
    </record>

    <!-- Task Manager Kanban -->
    <record id="view_task_manager_kanban" model="ir.ui.view">
        <field name="name">task.manager.kanban</field>
        <field name="model">task.manager</field>
        <field name="arch" type="xml">
            <kanban default_group_by="state">
                <field name="title"/>
                <field name="assigned_user_id"/>
                <field name="priority"/>
                <field name="state"/>
                <field name="color"/>
                <field name="is_overdue"/>
                <templates>
                    <t t-name="kanban-box">
                        <div t-attf-class="oe_kanban_card oe_kanban_global_click #{record.is_overdue.raw_value ? 'oe_kanban_card_danger' : ''}">
                            <div class="oe_kanban_content">
                                <div class="o_kanban_record_top">
                                    <div class="o_kanban_record_headings">
                                        <strong class="o_kanban_record_title"><field name="title"/></strong>
                                    </div>
                                    <div class="o_kanban_record_top_right">
                                        <div class="o_priority_star">
                                            <field name="priority" widget="priority"/>
                                        </div>
                                    </div>
                                </div>
                                <div class="o_kanban_record_body">
                                    <field name="description"/>
                                    <div t-if="record.vehicle_id.raw_value">
                                        🚗 <field name="vehicle_id"/>
                                    </div>
                                </div>
                                <div class="o_kanban_record_bottom">
                                    <div class="oe_kanban_bottom_left">
                                        <field name="assigned_user_id" widget="many2one_avatar_user"/>
                                    </div>
                                    <div class="oe_kanban_bottom_right">
                                        <field name="date_deadline" widget="date"/>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- Task Category Views -->
    <record id="view_task_category_form" model="ir.ui.view">
        <field name="name">task.category.form</field>
        <field name="model">task.category</field>
        <field name="arch" type="xml">
            <form string="Task Category">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="sequence"/>
                        <field name="color"/>
                        <field name="active"/>
                    </group>
                    <field name="description"/>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_task_category_list" model="ir.ui.view">
        <field name="name">task.category.list</field>
        <field name="model">task.category</field>
        <field name="arch" type="xml">
            <list string="Task Categories" editable="bottom">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="task_count"/>
                <field name="active"/>
            </list>
        </field>
    </record>

    <!-- Task Tag Views -->
    <record id="view_task_tag_form" model="ir.ui.view">
        <field name="name">task.tag.form</field>
        <field name="model">task.tag</field>
        <field name="arch" type="xml">
            <form string="Task Tag">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="color"/>
                        <field name="active"/>
                    </group>
                    <field name="description"/>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_task_tag_list" model="ir.ui.view">
        <field name="name">task.tag.list</field>
        <field name="model">task.tag</field>
        <field name="arch" type="xml">
            <list string="Task Tags" editable="bottom">
                <field name="name"/>
                <field name="task_count"/>
                <field name="active"/>
            </list>
        </field>
    </record>

    <!-- Actions -->
    <record id="action_task_manager" model="ir.actions.act_window">
        <field name="name">Tasks</field>
        <field name="res_model">task.manager</field>
        <field name="view_mode">kanban,list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first task!
            </p>
            <p>
                Use tasks to organize work and track progress.
            </p>
        </field>
    </record>

    <record id="action_task_category" model="ir.actions.act_window">
        <field name="name">Task Categories</field>
        <field name="res_model">task.category</field>
        <field name="view_mode">list,form</field>
    </record>

    <record id="action_task_tag" model="ir.actions.act_window">
        <field name="name">Task Tags</field>
        <field name="res_model">task.tag</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
